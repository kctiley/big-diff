'use strict'

const assert = require('assert')
const Diff = require('../')
const a = require('./a.json')
const b = require('./b.json')

describe('Diff', () => {
  it('', () => {
    let d = new Diff(a, b)
  })
})
